---
description: 用完整示例或交互资源做预测—验证—迁移
agent: course-tutor
---

加载 `code-lab-coach` skill，以 example 模式为 `$ARGUMENTS` 定位一个最小资源。讲次请求优先只检索 `opencode/lecture-runtime-index.jsonl` 的对应行。先让我预测或追踪，再给出 PAI-DSW Quickstart 中的 `run_example(...)` 或 `show_visualization(...)` 调用；等待我贴回 `[CS0502_RESULT]` 后再解释。不要在本机执行、复制或修改课程 Python。
