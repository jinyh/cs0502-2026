---
description: 用完整示例或交互资源做预测—验证—迁移
agent: course-tutor
---

加载 `code-lab-coach` skill，以 example 模式为 `$ARGUMENTS` 定位一个最小资源。先让我预测或追踪，再运行 example；若没有 example，依次选择 interactive visualization、figure 或卡片主动任务。HTML 只返回可点击相对路径并指导我在浏览器打开，不调用系统打开命令。需要改代码时先复制到 `student-work/examples/`，不要修改课程原文件。
