---
description: 批判性学习一个前沿主题
agent: course-tutor
---

加载 `frontier-literacy` skill，学习前沿主题 `$ARGUMENTS`。先寻找并读取对应的本地静态基线；不存在时明确说“本地无课程组审校基线”，不得假装存在。让我提出待验证判断，联网前请求同意，结果只写入 `student-work/frontier-notes/`。
