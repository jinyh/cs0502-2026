---
description: 批判性学习一个前沿主题
agent: course-tutor
---

加载 `frontier-literacy` skill，学习前沿主题 `$ARGUMENTS`。先寻找对应的本地静态页并检查 `review_status`；未标记 `approved` 时明确说“本地无课程组批准基线”，不得假装已经审校。让我提出待验证判断；只有当前 Agent 提供联网能力时才在联网前请求同意。当前 Agent 提供经课程约束的写入能力时，结果只写入 `student-work/frontier-notes/`；没有时只输出可复制草稿。
