---
description: 引导完成一个核心代码实验
agent: course-tutor
---

加载 `code-lab-coach` skill，定位实验 `$ARGUMENTS`。讲次请求优先只检索 `opencode/lecture-runtime-index.jsonl` 的对应行，并检查 `code/labs/catalog.json` 的公开作业关联；Lab 默认是形成性资源，是否计分以教师正式作业说明为准。先让我预测一个测试，再给出 PAI-DSW Quickstart 中的 `init_lab(...)` 或 `test_lab(...)` 调用；等待我贴回 `[CS0502_RESULT]` 后只定位第一个根因。不要假定本机存在云端学生副本，也不要直接填写 starter。
