# 代码区（code/）

可独立运行的示例与可视化，面向初学者风格，注重可读性，配合中文注释。

- `examples/` — 概念演示 Python 脚本，文件名前缀对应卡片编号（如 `04_stack_queue.py` ↔ `docs/cards/04-*.md`）
- `visualizations/` — 自包含 HTML 交互可视化（迁移自课程 Demo）

## 运行

```bash
uv venv && uv pip install -r requirements.txt
```

沙箱运行安全白名单见 `../opencode/sandbox-policy.md`。
